## Makefile.am -- Process this file with automake to process Makefile.in

unangband_files = \
  ai.txt \
  d_INFO2.txt \
  future.txt \
  k_info2.txt 

EXTRA_DIST = \
  $(unangband_files)

if SET_GID
unangbanddir = @DEFAULT_PATH@todo

unangband_DATA = \
  $(unangband_files)
endif


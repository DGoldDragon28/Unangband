## Makefile.am -- Process this file with automake to process Makefile.in

unangband_files = \
  sound.cfg

EXTRA_DIST = \
  $(unangband_files)

if SET_GID
unangbanddir = @DEFAULT_PATH@xtra@sound

unangband_DATA = \
  $(unangband_files)
endif


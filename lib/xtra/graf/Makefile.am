## Makefile.am -- Process this file with automake to process Makefile.in

unangband_files = \
  readme.txt

EXTRA_DIST = \
  $(unangband_files)

if SET_GID
unangbanddir = @DEFAULT_PATH@xtra@graf

unangband_DATA = \
  $(unangband_files)
endif

